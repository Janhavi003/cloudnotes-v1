apiVersion: v1
kind: Service
metadata:
  name: cloudnotes
  labels:
    app: cloudnotes
spec:
  type: ClusterIP
  selector:
    app: cloudnotes
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
